name: workflow.yml
on: push
#  schedule:
#    - cron: "0 */6 * * *"

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install .venv
        run: uv sync

      - name: Run
        run: uv run war_stats.py