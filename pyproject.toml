[project]
name = "clash-of-clans"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "irisMoonwhisper", email = "iris.moonwhisper@proton.me" }
]
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "requests>=2.34.2",
]

[project.scripts]
clash-of-clans = "clash_of_clans:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
